# Databases
go.db
python.db
node.db

# GO
# NOTE: go.sum is a lockfile and MUST be committed - the Docker build
# (COPY go.mod go.sum ./) fails on a fresh CI checkout without it.

# Python
venv/
__pycache__/
*.pyc

# NodeJS
node_modules/
package-lock.json
